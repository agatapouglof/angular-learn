import { DatelanguagePipe } from './datelanguage.pipe';

describe('DatelanguagePipe', () => {
  it('create an instance', () => {
    const pipe = new DatelanguagePipe();
    expect(pipe).toBeTruthy();
  });
});
